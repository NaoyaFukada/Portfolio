npm install --save-dev eslint-plugin-security
npm init @eslint/config@latest
npm install --save-dev eslint eslint-plugin-json
npm install @eslint/markdown -D

npm install -D vitest @testing-library/jest-dom jsdom
npm install vitest@2.0.5 --save-dev
# Dark Blue → #0c2d48

# Blue → #145da0

# Baby Blue → #2e8bc0

# Light Baby Blue → #b1d4e0
