---
import { Image } from 'astro:assets';
import profileImage from '../../assets/profile.jpg';
import SectionHeader from '../common/SectionHeader.astro';
import {aboutInfo} from '../../data/aboutInfo';
---

<section id="about" class="about">
  <SectionHeader 
    mainText="About" 
    highlightText="Me" 
    highlightClass="text-rose-400" 
    underlineClass="bg-rose-400" 
  />

  <div class="contents">
    <!-- Left Column -->
    <div class="group justify-self-center">
      <div class="img">
        <Image src={profileImage} alt="Naoya Fukada" loading="lazy" format="webp" class="w-full h-full object-cover z-100"></Image>
      </div>
    </div>

    <div class="justify-self-center space-y-6 max-w-lg">
      <div class="space-y-4">
          <h3 class="contents-title">
              My Journey into Software
          </h3>
          <p class="contents-text">
            I've always loved problem-solving and logical thinking—mathematics was my refuge 
            when other subjects felt overwhelming. This passion naturally led me to software 
            development, where logic meets creativity to solve real-world problems.
          </p>

          <p class="contents-text">
            Right after high school, I started learning to code independently. The satisfaction 
            of building working software was unlike anything I'd experienced before. Each project 
            deepened my passion for creating meaningful solutions through code.
          </p>
      </div>

      <div class="quickFacts">
        {aboutInfo.map((field) => (
           <div class="fact-item">
              <span class="text-xl">{field.icon}</span>
              <div>
                <span class="fact-title">{field.key}:</span>
                <span class="ml-2">{field.value}</span>
              </div>
            </div>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  @reference "tailwindcss";

  .about {
    @apply max-w-7xl mx-auto px-6 my-20 md:my-40 scroll-mt-20 z-1;
  }

  /* Contents */
  .contents {
    @apply grid md:grid-cols-2 gap-10 md:gap-12 items-start mt-16;
  }

  .img {
    @apply rounded-2xl overflow-hidden shadow-2xl transition-transform duration-500 group-hover:scale-[1.02] max-w-lg;
  }

  .contents-title {
    @apply text-3xl font-bold text-white mb-6;
  }

  .contents-text {
    @apply text-lg text-slate-300 leading-relaxed;
  }

  .quickFacts {
    @apply space-y-3;
  }

  .fact-item {
    @apply flex items-start gap-3 text-slate-300;
  }

  .fact-title {
    @apply font-semibold text-white;
  }
</style>